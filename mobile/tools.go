//go:build tools

// Пакет golang.org/x/mobile/bind не вызывается из нашего кода напрямую: его
// подставляет gomobile, когда собирает библиотеку для Android. Без этой ссылки
// go mod tidy вычёркивает зависимость, и сборка обрывается невнятным «нет
// такого пакета» — причём только на той машине, где собирают приложение.
package mobile

import _ "golang.org/x/mobile/bind"
