# Протокол локального сравнения 26 сентября 2026

- Начало итоговой серии: `2026-09-26T12:15:10Z`.
- CPU: `AMD Ryzen 7 7700 8-Core Processor`; 16 логических процессоров.
- ОС: `Microsoft Windows NT 10.0.26200.0`, amd64.
- Go: `go version go1.26.6 windows/amd64`; `GOMAXPROCS=16`.
- Код протоколов: `12d395a8cc2466bc6e758a6950fff0980664e076`.
- Стенд: новый `cmd/marvia-bench/main.go`, SHA-256
  `983309a9c013f0b236fe9ad96105e1af971355ab00613c3afdea6322ec37cdf1`.
- `GOCACHE` вынесен во временный каталог; `GOPROXY=off`. Сборка закончилась
  успешно; Go вывел предупреждение о невозможности записать stat cache модуля.
- Команда итогового измерения после сборки (PowerShell):

```powershell
& "$env:TEMP\marvia-bench.exe" -rounds 5 -mib 512 -echoes 1000 -revision 12d395a8cc2466bc6e758a6950fff0980664e076 |
  Set-Content -Encoding utf8 docs/benchmarks/2026-09-26-loopback.jsonl
```

Все 25 записей содержат `delivered_bytes=536870912`. Все 20 TLS-записей
содержат `tls_cipher=TLS_AES_128_GCM_SHA256`. Ошибок исполнения не было,
код возврата программы — 0. Каждый блок проверяется на равенство исходным
данным внутри измеряемого интервала.

Итоговая сводка строится группировкой по `mode`: сортировка пяти значений
`MB_per_second` даёт минимум, третий элемент (медиану) и максимум.
Аналогично считаются значения `echo_mean_us`. Никаких выброшенных наблюдений
в итоговой серии нет. Пробная серия до остановки эмулятора сохранена в
`2026-09-26-loopback-pilot.jsonl` и не смешана с итоговой.

Эмулятор QEMU остановлен до итогового запуска. Рабочий стол, браузер и фоновые
процессы не изолированы; нагрузка ОС и частоты процессора не зафиксированы.
Сравнение относится только к этому ПК и этим реализациям Marvia.
