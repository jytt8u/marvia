# Скрипты уходят на Linux как есть, в том числе вшитые в панель: CR в первой
# строке — и sh на ноде не найдёт интерпретатор. На Windows с autocrlf без
# этого файла они превращались бы в CRLF при каждом checkout.
*.sh text eol=lf
*.sh.tmpl text eol=lf
